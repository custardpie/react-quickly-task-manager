For this React project, always
* use functional components and typescript interfaces
* if a component exceeds 100 lines, suggest a refactor into subcomponents
* never use any; always define a proper type